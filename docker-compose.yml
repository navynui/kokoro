services:
  tts:
    build: .
    container_name: kokoro-tts
    ports:
      - "8001:8000"
    restart: unless-stopped
    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - ./output:/app/output
    # Uncomment the next two lines if you free up GPU headroom later:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
